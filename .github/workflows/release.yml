name: Release

on:
  push:
    tags: [ "*" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup JDK 11
        uses: actions/setup-java@v4
        with:
          java-version: 11
          distribution: zulu
          cache: gradle

      - name: Build
        run: |
          chmod +x gradlew
          ./gradlew :publishToMavenLocal --no-daemon

      - name: Publish release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          version="$(ls ~/.m2/repository/com/bugcord/axml/ | head -n1)"
          test -n "$version"

          files="$(find ~/.m2/repository/com/bugcord -type f \( -name '*.jar' -o -name '*.pom' -o -name '*.module' -o -name '*.aar' \) ! -name '*sources*' ! -name '*javadoc*' | tr '\n' ' ')"
          test -n "$files"

          gh release delete "$version" --cleanup-tag --yes || true
          gh release create "$version" $files \
            --title "$version" --notes "Built from $GITHUB_SHA"
